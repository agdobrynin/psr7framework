{
    "config": {
        "sort-packages": true
    },
    "require": {
        "php": ">=7.1.0",
        "roave/security-advisories": "dev-master"
    },
    "autoload": {
        "psr-4": {
            "Framework\\": "src/Framework/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^7.0"
    },
    "scripts": {
        "srv": "php -S 0.0.0.0:8080 -t public",
        "test": "phpunit --colors=always"
    }
}